#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jpm/bin/jpm" "$@"
  ret=$?
else 
  node  "$basedir/../jpm/bin/jpm" "$@"
  ret=$?
fi
exit $ret
